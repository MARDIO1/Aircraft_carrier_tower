# 使用说明
### 克隆后应该运行main文件或者uv run python src/main.py
  
* 进入后，默认为stop模式，空格是切换stop和auto其余每个模式的导航靠上下左右切换，[0,0],是stop，[1,0]是tower,建议在我没修好新的tower前不要使用，[2,0]是调参模式下设servo，pid和jacobi
* 切换模式都是调到相应目录然后按enter，调参模式的导航是可以正常使用的，调参模式的数字是可以键入的，auto只能预设状态，[3,0]是data模式收回传的黑匣子数据也可以在stop直接切d进入data
* 如果想要修改某个文件，inital是修改串口配置，protocol是所有状态机和协议数据包的存放处，send更多是一个象征意义文件，receive是接收文件如果改黑匣子数据就需要更改，teas和test都是测试用例不用管，crc校验在crc——calculator，consle是ui，blackbox——logger加入黑匣子