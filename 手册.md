# 飞行控制操作手册

## 快速上手

1. 进入系统后，按 `Space` 切换到 `AUTO`。
2. 按 `1` 激活控制。
3. 激活后可按 `2` 到 `9` 调整转速。
4. 在 `STOP` 模式下按 `D` 进入数据模式。
5. 任意时刻按 `Esc` 可快速退出当前模式。

## 按键速查

| 按键 | 功能 | 说明 |
|---|---|---|
| `Space` | 切换 `AUTO` | 进入后常用第一步 |
| `1` | 激活控制 | 激活后才能用转速档位键 |
| `2` ~ `9` | 调整转速 | 数字越大，转速档位越高 |
| `F9` | 舵机/舵面设置 | 用于舵机相关调节 |
| `F8` | `PID` 设置 | 用于 PID 参数调节 |
| `D` | 进入数据模式 | 需先处于 `STOP` 模式 |
| `Esc` | 通用退出 | 可理解为“万能退出键” |
| 方向键 | 切换其他模式 | 一般不常用，可忽略 |

## 推荐操作顺序

`Space` -> `1` -> `2~9` 调速 ->（需要调参时）`F8/F9` ->（需要记录数据时）回到 `STOP` 后按 `D`。

## 注意事项

- 进入数据模式前请确认当前为 `STOP`。
- 调速键 `2~9` 仅在已激活（按过 `1`）后生效。
- 如果界面状态混乱，优先按 `Esc` 退出，再重新按流程进入。




















































