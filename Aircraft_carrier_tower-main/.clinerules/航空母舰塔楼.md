# 这是一个航模地面站工程
* 注意使用了powershell终端
* 使用python进行项目。使用uv进行包管理。
* 使用uart 波特率115200和航模进行通讯
* 需要可快速配置的协议（层级比UART高）：

地面站发送 使用0xAA作为帧头 0xBB作为帧尾，中间包含float舵面，电机转速
航模发送 使用0xCC作为帧头 0xDD作为帧尾，中间包含回传的陀螺仪数据
* 注意上述信息可能变动，尽可能使用尽量简单的视线。不需要进行CRC校验。UART协议8N1。COM口应当可以手动配置
* 目前采用命令行控制，人工率很高，手敲麻了
